# --- LLM clients ---
openai>=1.60.0                # OpenAI-compatible SDK for Ark, DeepSeek, Walmart Gateway
pydantic-ai>=2.0.0            # PydanticAI framework for agent implementation

# --- Data sources ---
# NOTE: A-share data is now fetched directly from the public JSON endpoints
# that the eastmoney and cninfo websites use for their own front-ends —
# no third-party scraper (akshare / tushare) and no API token required.
# See DATA_SOURCES.md.
tavily-python>=0.5.0          # web search
trafilatura>=1.12.0           # HTML main-text extraction
requests>=2.32.0
lxml>=5.3.0

# --- Retrieval / ranking ---
rank-bm25==0.2.2

# --- Storage / data ---
pandas>=2.2.0
pyarrow>=18.0.0               # parquet for indices cache (py3.13 needs >=18)
tabulate>=0.9.0               # required by pandas.DataFrame.to_markdown()

# --- CLI / rendering ---
rich==13.9.4
click==8.1.7
markdown==3.7                 # md -> html
jinja2==3.1.4

# --- Utils ---
python-dotenv>=1.1.0
tenacity==9.0.0               # retries on API calls

# --- PDF processing ---
PyMuPDF>=1.23.0               # PDF text extraction (fitz)
